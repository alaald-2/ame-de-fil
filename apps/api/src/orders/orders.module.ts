import { Module } from "@nestjs/common";

// Order/OrderItem, the Order state machine (PAYMENTS.md §3). No
// controllers/services yet — full order lifecycle is a later phase.
@Module({})
export class OrdersModule {}
